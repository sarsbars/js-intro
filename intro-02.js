/*
    Variables, identifiers and naming conventions

    Vairables are containers for storing data. Variables can change.
    Identifiers are names given to variables, functions and other objects.

*/


var number = 7;
var name = "John Smith"; //This is a string
var valid = true;        // This is a boolean

var firstName = "John";
var userAddress = "123 Main St";
var lastName = "Smith";
var isValid = true;

console.log(number);
console.log(number + 10);

console.log(name + 7);

console.log("Welcome " + name + " it is so cold today");

/* It is bad practice to have several console.logs in your code, that is only
for beginners */

var $myValue = 17;
var _myValue = 17;

// You can start your variables with $ or _ but it is not recommended 

/* Javascript is dynamically, loosely typed language - you can change what 
information is in a variable, like switching from a single digit to several     
letters. We do have types, like boolean, number, text  but we do not declare them.
*/

var x = 5;
console.log(x);

x = "abomination";
console.log(x);

// Don't name your variables something so vague as "x"

