/* 
    Data Types

    Data types represent different kinds of values that can be used
    and manipulated.
        -Number (7, 15.9)
        -String ("sudu13r08f10ehf8 129r7y130d$")
        -Boolean (true or false)
        -BigInt
        -Symbol
        -undefined
        -null
        -object (an array is an object)

*/

// It is good convention to start vairables with "is" or "has"
var isValid = true;
var hasProperty = false;

//A classic JS object

const user = {
    firstName: 'John',
    lastName: 'Smith',
    age: 30
}

console.log(user.firstName);